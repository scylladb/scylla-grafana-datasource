import * as React from 'react';
import { API } from '@storybook/api';
interface DarkModeProps {
    api: API;
}
export declare const DarkMode: React.FunctionComponent<DarkModeProps>;
export default DarkMode;
