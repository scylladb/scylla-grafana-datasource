import { Field, Threshold, GrafanaTheme } from '../types';
export interface ScaledValue {
    percent?: number;
    threshold?: Threshold;
    color?: string;
}
export declare type ScaleCalculator = (value: number) => ScaledValue;
export declare function getScaleCalculator(field: Field, theme?: GrafanaTheme): ScaleCalculator;
export declare function getActiveThreshold(value: number, thresholds: Threshold[]): Threshold;
/**
 * Sorts the thresholds
 */
export declare function sortThresholds(thresholds: Threshold[]): Threshold[];
