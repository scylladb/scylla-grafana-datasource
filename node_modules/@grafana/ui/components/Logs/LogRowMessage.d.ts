import React from 'react';
import { LogRowModel } from '@grafana/data';
import { LogRowContextQueryErrors, HasMoreContextRows, LogRowContextRows } from './LogRowContextProvider';
import { Themeable } from '../../types/theme';
interface Props extends Themeable {
    row: LogRowModel;
    hasMoreContextRows?: HasMoreContextRows;
    contextIsOpen: boolean;
    wrapLogMessage: boolean;
    errors?: LogRowContextQueryErrors;
    context?: LogRowContextRows;
    showContextToggle?: (row?: LogRowModel) => boolean;
    highlighterExpressions?: string[];
    getRows: () => LogRowModel[];
    onToggleContext: () => void;
    updateLimit?: () => void;
}
export declare const LogRowMessage: React.FunctionComponent<Pick<Props, "context" | "row" | "errors" | "hasMoreContextRows" | "getRows" | "highlighterExpressions" | "contextIsOpen" | "wrapLogMessage" | "showContextToggle" | "onToggleContext" | "updateLimit">>;
export {};
