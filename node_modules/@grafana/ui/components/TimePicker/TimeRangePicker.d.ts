import React, { PureComponent } from 'react';
import { TimeRange, TimeZone } from '@grafana/data';
import { Themeable } from '../../types';
export interface Props extends Themeable {
    hideText?: boolean;
    value: TimeRange;
    timeZone?: TimeZone;
    timeSyncButton?: JSX.Element;
    isSynced?: boolean;
    onChange: (timeRange: TimeRange) => void;
    onMoveBackward: () => void;
    onMoveForward: () => void;
    onZoom: () => void;
    history?: TimeRange[];
}
export interface State {
    isOpen: boolean;
}
export declare class UnthemedTimeRangePicker extends PureComponent<Props, State> {
    state: State;
    onChange: (timeRange: TimeRange) => void;
    onOpen: (event: React.FormEvent<HTMLButtonElement>) => void;
    onClose: () => void;
    render(): JSX.Element;
}
export declare const TimeRangePicker: React.FunctionComponent<Pick<Props, "onChange" | "history" | "timeZone" | "value" | "hideText" | "timeSyncButton" | "isSynced" | "onMoveBackward" | "onMoveForward" | "onZoom">>;
