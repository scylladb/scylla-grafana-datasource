import { GrafanaTheme } from '@grafana/data';
export declare const getThemeColors: (theme: GrafanaTheme) => {
    border: string;
    background: string;
    shadow: string;
    formBackground: string | number;
};
